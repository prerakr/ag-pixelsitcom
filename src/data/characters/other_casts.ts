import { CharacterDefinition } from '../../types/character';

// Guest characters registry for special crossovers or future expansions
export const OTHER_CAST: Record<string, CharacterDefinition> = {};
